name: rbws
description: really bad web server (another generic http/1.1 server implementation)
version: 2.0.0
repository: https://github.com/masonfrykman/rbws

topics:
  - http
  - server

environment:
  sdk: ^3.2.0

dependencies:
  mime: ^1.0.5

dev_dependencies:
  archive: ^3.6.1
  lints: ^3.0.0
  test: ^1.25.7

platforms:
  linux:
  macos:
  windows: